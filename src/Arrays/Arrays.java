package Arrays;

public class Arrays {
    private static Object Arrays;

    public static void main(String[] args) {

  //we use collection of the same kind
  // syntax -->  data type [] var.name = new dataType [size];
        //[size] an integer that tells us how many things the array will store
        //String [] names = new String [4];
        //each part of an array is called an element;
        //when you make a new String array all the elements are null
        //when new integer in to array all the elements are 0;
        // every element is an index;
        // how to store info into array
        //nameOfArray[index] = value;
        //names [0] = name1; -->names[0] = "james"
        //names [1] = name2; -->name[1] = "adam"
        // the value being stored into array should be the same data type of the array being storing into
// printing the whole array System.out.println(Array.toString (nums )
        //arrays to STRING
        //s
int [] nums = new int [6];
nums [0] = 4;
nums [1] =3;
nums [2] = 6;
nums[2]= nums[2]+1;
nums [3]= nums[0]+nums[1];

        System.out.println(nums[3]);
//the amount of time we should loop
//how many interations do we need to go throuth the whole array -->lenght
 // length () vs length
 // lenght --> lenght of array (how many elements

    }
}
